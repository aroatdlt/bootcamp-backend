{
  "name": "02-playground-typescript",
  "version": "1.0.0",
  "description": "Playground to show code snippets via TypeScript",
  "type": "module",
  "scripts": {
    "start": "nodemon --quiet --watch ./src --ext ts --exec node  --experimental-specifier-resolution=node --loader=ts-node/esm --no-warnings ./src/exercise5.ts"
  },
  "devDependencies": {
    "@types/node": "^14.17.3",
    "nodemon": "^2.0.7",
    "ts-node": "^10.4.0",
    "typescript": "^4.3.4"
  }
}
